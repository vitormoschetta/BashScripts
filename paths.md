Para criar um diretório e dar permissões usando um arquivo .sh (bash):

```
sudo mkdir -p ./backups/download/
sudo mkdir -p ./backups/upload/
sudo chmod +rxw backups/download/
sudo chmod +rxw backups/upload/
```
